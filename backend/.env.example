# Backend Environment Variables

# Server Port
PORT=5000

# MongoDB Connection URI
# For local MongoDB: mongodb://localhost:27017/your_database_name
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGO_URI=mongodb://localhost:27017/consultancy_db

# JWT Secret Key (generate a random secure string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=6f6fafb4e1f2b48ed6d07eaad73fe59589feee820af333153daed8f2b93606f3

# Email Configuration (Gmail)
# To use Gmail:
# 1. Go to Google Account Settings
# 2. Enable 2-Factor Authentication
# 3. Generate an App Password: https://myaccount.google.com/apppasswords
# 4. Use the generated password below
EMAIL_USER=mithuld321@gmail.com
EMAIL_PASS=gxorrwfinrqrfucw

# Client/Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173

# Admin Access Key (set a secure key for admin login)
# This key is required along with regular credentials to access admin panel
ADMIN_KEY=admin123secret

# ============================================
# GOOGLE OAUTH CONFIGURATION
# ============================================
# Get these credentials from: https://console.cloud.google.com/apis/credentials
# Follow the setup guide in GOOGLE_OAUTH_SETUP_GUIDE.md

# Google OAuth Client ID (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# Google OAuth Client Secret (from Google Cloud Console)
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret-here

# OAuth Redirect URI (must match Google Cloud Console configuration)
# Development:
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback
# Production example:
# GOOGLE_REDIRECT_URI=https://api.yourdomain.com/api/auth/google/callback

# Frontend URL (for post-authentication redirects)
FRONTEND_URL=http://localhost:5174
